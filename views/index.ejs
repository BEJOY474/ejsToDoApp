<%- include('layout/header') %>

    <main>
        <form action="/" method="post">
            <input type="text" name="programmingLanguage" placeholder="Programming language" id="">
            <button type="submit">Add</button>
        </form>

        <ul>
            <!-- <li><%= allProgName %></li> -->
             <!-- <% if(allProgName.length > 0){ %>
                <li><%= allProgName %></li>
            <% }else{ %>
                <p>No Data Available</p>
            <% } %> -->

              <% if(allProgName.length > 0){ %>
                <%for(let i=0; i < allProgName.length; i++){%>
                    <li><%= allProgName[i] %></li>
                <% } %>
               
            <% }else{ %>
                <p>No Data Available</p>
            <% } %>

        </ul>

    </main>

<%- include("layout/footer") %>